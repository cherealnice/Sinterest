{
  "name": "Sinterest",
  "dependencies": {
    "browserify-incremental": "^3.0.0",
    "reactify": "^1.1.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">= 0.10"
  },
  "dependencies": {
    "browserify": "^12.0.1"
  },
  "scripts" : {
      "build" : "npm run build-js && npm run build-css"
  }
}
